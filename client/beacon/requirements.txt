# DonWatcher Beacon Agent Dependencies
# Note: The beacon can run with just Python stdlib (urllib)
# but requests is recommended for better error handling

requests>=2.28.0

# For building standalone executable (optional)
# Run: pip install pyinstaller
# Then: python build.py --server YOUR_SERVER_URL --output beacon.exe
# pyinstaller>=5.0

